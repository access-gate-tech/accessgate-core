{
  "name": "accessgate-core",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only src/main.ts",
    "build": "tsc",
    "test": "vitest",
    "test:watch": "vitest --watch",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "@fastify/cors": "^11.0.1",
    "@fastify/env": "^5.0.2",
    "@fastify/helmet": "^13.0.1",
    "fastify": "^5.3.2",
    "zod": "^3.22.2"
  },
  "devDependencies": {
    "@types/node": "^20.4.2",
    "pino-pretty": "^13.0.0",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.2.2",
    "vite": "^6.3.3",
    "vite-node": "^3.1.2",
    "vitest": "^3.1.2",
    "@vitest/expect": "^3.1.2",
    "@vitest/coverage-v8": "^3.1.2"
  }
}
